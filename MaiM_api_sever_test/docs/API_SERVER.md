# MaiMBot API Server 架构设计文档

## 概述

MaiMBot API Server 采用三层架构设计，将系统分为配置器和回复后端，通过前端界面完成用户管理、Agent配置和聊天功能的完整生态。

## 架构概览

```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   前端界面       │    │  配置器后端      │    │   回复后端       │
│  (Config Frontend)◄──►│(Config Backend) ◄──►│ (Reply Backend) │
│                 │    │                 │    │                 │
│ - 用户注册登录   │    │ - 用户管理API    │    │ - bot.py        │
│ - Agent配置界面  │    │ - Agent管理API   │    │ - 聊天处理      │
│ - 系统管理       │    │ - 租户管理API    │    │ - 消息路由      │
└─────────────────┘    └─────────────────┘    └─────────────────┘
         │                       │                       │
         └───────────────────────┼───────────────────────┘
                                 │
                    ┌─────────────────┐
                    │   数据库层       │
                    │  (Database)     │
                    │                 │
                    │ - tenant_users  │
                    │ - agent_records │
                    │ - agent_templates│
                    │ - 配置数据       │
                    └─────────────────┘
```

## 核心组件详解

### 1. 配置器后端 (Config Backend)

配置器后端负责用户管理和Agent配置的核心业务逻辑，是整个系统的配置管理中心。

#### 主要功能

**用户管理模块**:
- 用户注册 (`POST /api/v1/auth/register`)
- 用户登录 (`POST /api/v1/auth/login`)
- 用户认证 (`JWT Token + API Key`)
- 会话管理 (`UserSessions`)
- 租户信息管理 (`TenantUsers`)

**Agent配置模块**:
- Agent模板管理 (`AgentTemplates`)
- Agent创建 (`POST /api/v1/agents`)
- Agent配置更新 (`PUT /api/v1/agents/{agent_id}`)
- 租户级别Agent隔离

**数据流向**:
```
前端请求 → 配置器后端API → 数据库写入(TenantUsers/AgentRecord) → 返回结果
```

#### 核心数据库表

**TenantUsers表** (租户用户):
```sql
- tenant_id (租户ID，唯一)
- user_id (用户ID，唯一)
- username (用户名)
- email (邮箱)
- password_hash (密码哈希)
- api_key (API密钥)
- tenant_name (租户名称)
- tenant_type (租户类型: personal/enterprise)
- status (状态: active/suspended/deleted)
```

**AgentRecord表** (Agent配置):
```sql
- tenant_id (租户ID，关联TenantUsers)
- agent_id (Agent ID，租户内唯一)
- name (Agent名称)
- description (描述)
- persona (人格描述)
- bot_overrides (配置覆盖)
- config_overrides (配置覆盖)
```

**AgentTemplates表** (Agent模板):
```sql
- template_id (模板ID，唯一)
- name (模板名称)
- category (分类: general/professional/entertainment)
- persona (人格描述)
- personality_traits (人格特征JSON)
- response_style (回复风格)
- memory_config (记忆配置JSON)
- plugin_config (插件配置JSON)
```

### 2. 回复后端 (Reply Backend)

回复后端负责实际的聊天处理和消息回复，以数据库中的配置为真实数据源。

#### 主要特点

**配置读取优先级**:
1. **数据库配置** (最高优先级，运行时真实值)
2. **配置文件** (仅在开机时写入数据库，用于调试)

**核心处理流程**:
```
用户消息 → 回复后端 → 查询数据库配置 → 加载Agent → 生成回复 → 返回结果
```

**实现组件**:
- `bot.py` - 主入口点
- 聊天流管理 (`ChatStreams`)
- 消息处理 (`Messages`)
- 插件系统 (Plugins)
- 记忆系统 (Memory)

### 3. 前端界面 (Config Frontend)

前端提供用户友好的配置界面，完成所有配置器相关的操作。

#### 主要界面

**用户管理界面**:
- 注册/登录页面
- 用户信息管理
- 租户配置
- API密钥管理

**Agent配置界面**:
- Agent模板选择
- Agent创建向导
- 人格配置
- 插件配置
- 记忆设置

**系统管理界面**:
- 租户统计
- 使用监控
- 配置备份
- 系统状态

## 数据流向设计

### 配置流程
```
1. 前端发起配置请求
   ↓
2. 配置器后端接收请求
   ↓
3. 验证用户权限
   ↓
4. 写入数据库(TenantUsers/AgentRecord)
   ↓
5. 返回配置结果
   ↓
6. 前端显示成功
```

### 聊天流程
```
1. 前端发起聊天请求(带tenant_id/agent_id)
   ↓
2. 回复后端接收请求
   ↓
3. 查询数据库获取Agent配置
   ↓
4. 加载对应Agent实例
   ↓
5. 处理消息生成回复
   ↓
6. 返回聊天结果
   ↓
7. 前端显示回复
```

## API接口设计

### 配置器后端API

**认证相关**:
- `POST /api/v1/auth/register` - 用户注册
- `POST /api/v1/auth/login` - 用户登录
- `GET /api/v1/auth/me` - 获取用户信息
- `POST /api/v1/auth/logout` - 用户登出

**租户管理**:
- `GET /api/v1/tenant` - 获取租户信息
- `PUT /api/v1/tenant` - 更新租户配置
- `GET /api/v1/tenant/stats` - 租户统计

**Agent管理**:
- `GET /api/v1/agents/templates` - 获取模板列表
- `POST /api/v1/agents` - 创建Agent
- `GET /api/v1/agents` - 获取Agent列表
- `PUT /api/v1/agents/{agent_id}` - 更新Agent
- `DELETE /api/v1/agents/{agent_id}` - 删除Agent

### 回复后端API

**聊天功能**:
- `POST /api/v2/chat` - 发送消息(请求体参数)
- `POST /api/v2/chat/auth` - 认证用户聊天
- `POST /api/v2/chat/batch` - 批量聊天
- `GET /api/v2/agents` - 获取可用Agent

## 部署架构

### 开发环境
```
┌─────────────────┐
│   前端开发服务器  │ :3000
└─────────┬───────┘
          │ HTTP
┌─────────▼───────┐
│  配置器后端API    │ :8000
│  (FastAPI)       │
└─────────┬───────┘
          │ 数据库
┌─────────▼───────┐
│   SQLite数据库   │
└─────────────────┘

┌─────────────────┐
│   回复后端       │ :8095
│   (bot.py)       │
└─────────┬───────┘
          │ 数据库
┌─────────▼───────┐
│   SQLite数据库   │
└─────────────────┘
```

### 生产环境
```
┌─────────────────┐
│   负载均衡器      │
└─────────┬───────┘
          │
    ┌─────┴─────┐
    │           │
┌───▼───┐   ┌───▼───┐
│前端集群│   │API集群 │
└───┬───┘   └───┬───┘
    │           │
    └─────┬─────┘
          │
┌─────────▼───────┐
│   数据库集群      │
│ (PostgreSQL)    │
└─────────────────┘

┌─────────────────┐
│  回复后端集群     │
│  (多个bot.py实例) │
└─────────┬───────┘
          │
┌─────────▼───────┐
│   缓存集群       │
│   (Redis)       │
└─────────────────┘
```

## 关键设计原则

### 1. 数据库优先原则
- **配置器后端**: 所有配置写入数据库作为真实数据源
- **回复后端**: 始终从数据库读取配置，确保数据一致性
- **配置文件**: 仅用于系统初始化和调试，不影响运行时配置

### 2. 多租户隔离原则
- **租户级隔离**: 每个租户的数据完全隔离
- **Agent级隔离**: 租户内的Agent配置相互独立
- **权限控制**: 基于JWT Token和API Key的细粒度权限控制

### 3. 配置与运行分离原则
- **配置器**: 专注于配置管理，不处理聊天逻辑
- **回复器**: 专注于消息处理，不涉及配置变更
- **前端**: 提供配置界面，不直接操作数据库

### 4. 向后兼容原则
- **API版本**: 同时支持v1(传统)和v2(新版)API
- **数据迁移**: 支持从配置文件到数据库的平滑迁移
- **渐进升级**: 支持系统的逐步升级和迁移

## 实现状态

### ✅ 已完成组件

**数据库模型**:
- `TenantUsers` - 租户用户管理 ✅
- `AgentRecord` - Agent配置管理 ✅
- `AgentTemplates` - Agent模板系统 ✅
- `UserSessions` - 会话管理 ✅

**配置器后端API**:
- 用户注册/登录/认证 ✅
- 租户管理接口 ✅
- Agent模板管理 ✅
- Agent创建/更新/删除 ✅

**回复后端**:
- bot.py (现有实现) ✅
- 聊天API v2 (请求体参数) ✅
- 多租户聊天支持 ✅

**认证系统**:
- JWT Token认证 ✅
- API Key认证 ✅
- 请求体参数认证 ✅

### 🚧 待完善组件

**前端界面**:
- 用户注册/登录界面
- Agent配置界面
- 租户管理界面
- 系统监控界面

**系统功能**:
- 配置备份/恢复
- 系统监控告警
- 使用统计分析
- 性能优化

## 使用指南

### 1. 启动配置器后端
```bash
cd /path/to/MaiMBot
python -m src.api.main
# 配置器API将在 http://localhost:8000 启动
```

### 2. 启动回复后端
```bash
cd /path/to/MaiMBot
python bot.py
# 回复后端将在 http://localhost:8095 启动
```

### 3. 数据库初始化
```python
# 首次运行时自动初始化数据库表
from src.api.init_agent_templates import init_template_data
init_template_data()  # 初始化系统Agent模板
```

### 4. API使用示例

**用户注册**:
```bash
curl -X POST "http://localhost:8000/api/v1/auth/register" \
  -H "Content-Type: application/json" \
  -d '{"username":"testuser","password":"password123","email":"test@example.com"}'
```

**创建Agent**:
```bash
curl -X POST "http://localhost:8000/api/v1/agents" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"name":"我的助手","template_id":"friendly_assistant"}'
```

**发送聊天消息**:
```bash
curl -X POST "http://localhost:8095/api/v2/chat" \
  -H "Content-Type: application/json" \
  -d '{"message":"你好","tenant_id":"tenant_123","agent_id":"agent_456"}'
```

## 总结

MaiMBot API Server的三层架构设计实现了配置与运行的完全分离，通过数据库作为单一数据源确保了数据一致性，同时提供了完整的多租户隔离和用户管理功能。这种架构既保证了系统的可扩展性，又简化了维护复杂度，为MaiMBot的企业级部署提供了坚实的基础。